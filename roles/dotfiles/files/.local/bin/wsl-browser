#!/bin/bash

WEB_BROWSER="/mnt/c/Program Files/Mozilla Firefox/firefox.exe"

if [[ $1 =~ ^https?:// ]]; then
    "$WEB_BROWSER" -url "$1"
else
    winpath=$(wslpath -w "$1")
    "$WEB_BROWSER" -url "$winpath"
fi
